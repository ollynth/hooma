$kind: http-request
name: Login Customer
url: "{{BASE_URL}}/auth/login"
method: POST
body:
  type: json
  content: "{\r

    \    \"username\" : \"lalaxio7395\",\r

    \    \"password\" : \"Lala1234\"\r

    }"
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: "const authToken = pm.cookies.get(\"authToken\");\r

      pm.environment.set(\"CUST_TOKEN\", authToken);\r

      \r

      const csrfToken = pm.cookies.get(\"csrfToken\");\r

      if (csrfToken) {\r

      \    pm.collectionVariables.set(\"csrfToken\", csrfToken);\r

      }\r

      \r\n"
    language: text/javascript
settings:
  disableCookies: false
order: 2000
